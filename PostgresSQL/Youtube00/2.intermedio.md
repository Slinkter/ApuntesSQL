# SQL Intermedio con PostgreSQL: Lógica de Negocio y Operaciones de Conjunto

> **Visión del Principal Data Architect:** Aquí dominamos la lógica de negocio compleja. Un JOIN mal construido puede generar un producto cartesiano de millones de filas, matando el rendimiento.
>
> **Conexión con el Prompt:** Este nivel introduce conceptos de arquitectura relacional - cómo reconstruir el modelo de negocio desde múltiples tablas.
>
> Revisa `02_Postgres_Internals/Clase_2.01_Postgres_Internals.md` (MVCC, Vacuum, Heap Pages) antes de ejecutar estos ejercicios.

> **Conexión con Internals:** Estos ejercicios aplican conceptos de `02_Postgres_Internals/Clase_2.01_Postgres_Internals.md` (MVCC, Vacuum, Heap Pages).
>
> En esta fase se abordan estructuras de consulta de mayor complejidad, enfocadas en el análisis relacional profundo y el manejo de nulidad mediante lógica avanzada en **PostgreSQL**.

## 1. Uniones de Tablas Avanzadas (`JOIN`)

### `LEFT JOIN`

> **PostgreSQL Tip:** El orden de las tablas en el JOIN afecta el resultado. LEFT JOIN preserva todas las filas de la tabla izquierda.
>
> **Por qué usarlo:** Cuando necesitas saber qué clientes NO han realizado pedidos (no aparecen en orders con INNER JOIN).

1. **Listar todos los clientes y los pedidos que han realizado. Incluir clientes que no han hecho pedidos.**

    ```sql
    SELECT c.contact_name, o.order_id
    FROM customers c
    LEFT JOIN orders o ON c.customer_id = o.customer_id;
    ```

    -- Resultado: Todos los clientes, incluso los que no tienen pedidos (order_id será NULL)

2. **Mostrar todos los productos y, si han sido pedidos, la cantidad vendida. Incluir productos nunca vendidos.**

    ```sql
    SELECT p.product_name, od.quantity
    FROM products p
    LEFT JOIN order_details od ON p.product_id = od.product_id;
    ```

    -- Productos sin venta tendrán quantity NULL

### `RIGHT JOIN`

> **PostgreSQL Tip:** RIGHT JOIN es menos común porque puedes reordenar las tablas y usar LEFT JOIN. Pero existe para casos específicos.

**Objetivo:** Devolver todas las filas de la tabla derecha y las filas coincidentes de la tabla izquierda. Es menos común que `LEFT JOIN`.

1. **Listar todos los empleados y los pedidos que han gestionado. Incluir empleados que no han gestionado pedidos.**

    ```sql
    SELECT e.first_name, e.last_name, o.order_id
    FROM orders o
    RIGHT JOIN employees e ON o.employee_id = e.employee_id;
    ```

    -- Note: Es más legible写成 LEFT JOIN invertiendo el orden:
    -- SELECT e.first_name, e.last_name, o.order_id
    -- FROM employees e LEFT JOIN orders o ON e.employee_id = o.employee_id;

### `JOIN` con Múltiples Tablas

**Objetivo:** Combinar datos de tres o más tablas.

1. **Para el pedido 10250, mostrar el nombre del cliente, el nombre del producto y la cantidad.**

    ```sql
    SELECT c.contact_name, p.product_name, od.quantity
    FROM orders o
    JOIN customers c ON o.customer_id = c.customer_id
    JOIN order_details od ON o.order_id = od.order_id
    JOIN products p ON od.product_id = p.product_id
    WHERE o.order_id = 10250;
    ```

2. **Mostrar el nombre del cliente, el empleado que lo atendió y el transportista para cada pedido.**

    ```sql
    SELECT o.order_id, c.contact_name, e.first_name AS employee, s.company_name AS shipper
    FROM orders o
    JOIN customers c ON o.customer_id = c.customer_id
    JOIN employees e ON o.employee_id = e.employee_id
    JOIN shippers s ON o.ship_via = s.shipper_id;
    ```

## 2. Subconsultas y Abstracción de Datos (Subqueries)

**Objetivo Académico:** Utilizar resultados de una consulta como entrada para otra, permitiendo realizar comparaciones dinámicas y particionar la lógica de la petición.

### Subconsulta en la Cláusula `WHERE`

> **PostgreSQL Tip:** Subconsultas en WHERE se evalúan primero. Para tablas grandes, considera usar JOIN o CTE.
>
> **Ejecución:** Cuando la subconsulta devuelve múltiples valores, usa `IN`. Para un solo valor, puedes usar `=`.

1. **Encontrar todos los productos suministradoados por proveedores de Japón.**

    ```sql
    SELECT product_name
    FROM products
    WHERE supplier_id IN (
        SELECT supplier_id 
        FROM suppliers 
        WHERE country = 'Japan'
    );
    ```

2. **Listar los clientes que han realizado pedidos después del '1998-03-01'.**

    ```sql
    SELECT contact_name
    FROM customers
    WHERE customer_id IN (
        SELECT customer_id 
        FROM orders 
        WHERE order_date > '1998-03-01'
    );
    ```

### Subconsulta en la Cláusula `FROM`

> **PostgreSQL Tip:** Una subconsulta en FROM actúa como una tabla virtual. Debe tener un alias (AS).

1. **Calcular el promedio de la cantidad de productos por pedido.**

    ```sql
    SELECT AVG(total_products)
    FROM (
        SELECT order_id, COUNT(product_id) AS total_products 
        FROM order_details 
        GROUP BY order_id
    ) AS order_counts;  -- IMPORTANTE: Todo subconsulta en FROM necesita alias

2. **Listar los clientes que han realizado pedidos después del '1998-03-01'.**

    ```sql
    SELECT contact_name
    FROM customers
    WHERE customer_id IN (SELECT customer_id FROM orders WHERE order_date > '1998-03-01');
    ```

### Subconsulta en la Cláusula `FROM`

1. **Calcular el promedio de la cantidad de productos por pedido.**

    ```sql
    SELECT AVG(total_products)
    FROM (SELECT order_id, COUNT(product_id) AS total_products FROM order_details GROUP BY order_id) AS order_counts;
    ```

### Subconsulta Correlacionada

> **PostgreSQL Tip:** Las subconsultas correlacionadas pueden ser lentas en tablas grandes. Considera usar `LATERAL JOIN` o CTEs para mejor rendimiento.

1. **Encontrar productos cuyo precio es superior al precio promedio de los productos de su misma categoría.**

    ```sql
    SELECT product_name, unit_price
    FROM products p1
    WHERE p1.unit_price > (SELECT AVG(unit_price) FROM products p2 WHERE p2.category_id = p1.category_id);
    ```

44. **(Extra) Encontrar los pedidos que contienen el producto 'Chai'**

    ```sql
    SELECT * FROM orders
    WHERE order_id IN (
        SELECT order_id FROM order_details
        WHERE product_id = (SELECT product_id FROM products WHERE product_name = 'Chai')
    );
    ```

## 3. Filtrado de Grupos (`HAVING`)

> **PostgreSQL Tip:** HAVING filtra después del GROUP BY. WHERE filtra antes. Por eso puedes usar funciones de agregación en HAVING pero no en WHERE.
>
> **Orden de ejecución:** FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY

> **Por qué HAVING ≠ WHERE:** No puedes usar `COUNT(*) > 5` en WHERE porque COUNT es una agregación que opera sobre grupos, no filas individuales.

1. **Encontrar países con más de 5 clientes.**

    ```sql
    SELECT country, COUNT(*) AS customer_count
    FROM customers
    GROUP BY country
    HAVING COUNT(*) > 5;
    ```

2. **Listar los empleados que han gestionado más de 80 pedidos.**

    ```sql
    SELECT employee_id, COUNT(order_id) AS order_count
    FROM orders
    GROUP BY employee_id
    HAVING COUNT(order_id) > 80;
    ```

3. **Mostrar las categorías donde el precio promedio del producto es mayor a $30.**

    ```sql
    SELECT category_id, AVG(unit_price) AS avg_price
    FROM products
    GROUP BY category_id
    HAVING AVG(unit_price) > 30;
    ```

47. **(Extra) Mostrar los proveedores que suministran más de 3 productos.**

    ```sql
    SELECT s.contact_name, COUNT(p.product_id) AS number_of_products
    FROM suppliers s
    JOIN products p ON s.supplier_id = p.supplier_id
    GROUP BY s.supplier_id, s.contact_name
    HAVING COUNT(p.product_id) > 3;
    ```

## 4. Errores Comunes con HAVING

> **Error típico:** Usar WHERE con agregados (no funciona - el grupo no existe todavía cuando WHERE se ejecuta).

1. **INCORRECTO:**

    ```sql
    -- Error: aggregate function calls cannot be used in WHERE
    SELECT country, COUNT(*)
    FROM customers
    WHERE COUNT(*) > 5
    GROUP BY country;
    ```

2. **CORRECTO (usar HAVING):**

    ```sql
    SELECT country, COUNT(*)
    FROM customers
    GROUP BY country
    HAVING COUNT(*) > 5;
    ```

3. **INCORRECTO (WHERE después de GROUP BY):**

    ```sql
    -- Error: column reference must be in aggregate function
    SELECT country, COUNT(*)
    FROM customers
    GROUP BY country
    WHERE country != 'USA';  -- Use HAVING para filtrar grupos
    ```

4. **CORRECTO (WHERE antes de GROUP BY - filtra filas):**

    ```sql
    SELECT country, COUNT(*)
    FROM customers
    WHERE country IS NOT NULL  -- Filtra filas ANTES de grouping
    GROUP BY country
    HAVING COUNT(*) > 3;  -- Filtra grupos DESPUÉS de grouping
    ```

## 4. `CASE` Statement

**Objetivo:** Añadir lógica condicional (if-then-else) a las consultas.

1. **Clasificar productos por precio: 'Barato' si es < $20, 'Medio' si es entre $20 y $50, 'Caro' si es > $50.**

    ```sql
    SELECT product_name, unit_price,
           CASE
               WHEN unit_price < 20 THEN 'Barato'
               WHEN unit_price BETWEEN 20 AND 50 THEN 'Medio'
               ELSE 'Caro'
           END AS price_category
    FROM products;
    ```

2. **Mostrar el nombre del cliente y un comentario sobre su ubicación ('Local' si es de USA, 'Extranjero' si no).**

    ```sql
    SELECT contact_name, country,
           CASE
               WHEN country = 'USA' THEN 'Local'
               ELSE 'Extranjero'
           END AS location
    FROM customers;
    ```

3. **(Extra) Contar pedidos por prioridad de envío ('Normal', 'Urgente' si es de USA).**

    ```sql
    SELECT order_id, customer_id,
           CASE
               WHEN ship_country = 'USA' THEN 'Urgente'
               ELSE 'Normal'
           END AS shipping_priority
    FROM orders;
    ```

## 5. Expresiones de Tabla Comunes (CTEs)

> **PostgreSQL Tip:** Las CTEs son optimizadas en PostgreSQL. Pueden mejorar significativamente el plan de ejecución vs subconsultas en FROM.
>
> **Conexión con Fundamentos:** Una CTE bien diseñada puede reducir el número de páginas leídas si filtra datos temprano.

1. **Usar una CTE para listar los clientes de Londres y luego mostrar sus pedidos.**

    ```sql
    WITH london_customers AS (
        SELECT customer_id, contact_name
        FROM customers
        WHERE city = 'London'
    )
    SELECT lc.contact_name, o.order_id
    FROM london_customers lc
    JOIN orders o ON lc.customer_id = o.customer_id;
    ```

2. **Encontrar el total de ventas por cada categoría de producto usando una CTE.**

    ```sql
    WITH product_sales AS (
        SELECT p.category_id, od.quantity * od.unit_price AS sale_amount
        FROM order_details od
        JOIN products p ON od.product_id = p.product_id
    )
    SELECT cat.category_name, SUM(ps.sale_amount) AS total_sales
    FROM product_sales ps
    JOIN categories cat ON ps.category_id = cat.category_id
    GROUP BY cat.category_name
    ORDER BY total_sales DESC;
    ```

3. **(Extra) Listar los 5 clientes con mayor gasto total.**

    ```sql
    WITH customer_spending AS (
        SELECT c.customer_id, c.contact_name, SUM(od.quantity * od.unit_price) AS total_spent
        FROM customers c
        JOIN orders o ON c.customer_id = o.customer_id
        JOIN order_details od ON o.order_id = od.order_id
        GROUP BY c.customer_id, c.contact_name
    )
    SELECT contact_name, total_spent
    FROM customer_spending
    ORDER BY total_spent DESC
    LIMIT 5;
    ```

## 6. Funciones de Ventana (Analytic Window Functions)

> **PostgreSQL Tip:** Las funciones de ventana no agrupan datos - mantienen cada fila individual mientras calculan sobre un "ventana" de filas relacionadas.
>
> **Diferencia clave vs GROUP BY:**
>
> - GROUP BY: Colapsa filas en una por grupo
> - Window Functions: Mantiene cada fila + calcula sobre un grupo sin colapsar
>
> **Partes de una función de ventana:**
>
> - `OVER()`: Define la ventana (si vacío, usa todas las filas)
> - `PARTITION BY`: Divide en grupos (como GROUP BY pero sin colapsar)
> - `ORDER BY`: Define el orden dentro de cada partición
> - `ROWS/RANGE`: Marco de ventana (filas específicas)

**Objetivo Académico:** Realizar cálculos sobre un subconjunto de filas relacionadas con la fila actual sin colapsar el resultado en un solo grupo. Son herramientas esenciales para el análisis de series temporales y rankings competitivos.

### `ROW_NUMBER`, `RANK`, `DENSE_RANK`

> **Diferencia entre las funciones:**
>
> - `ROW_NUMBER()`: 1, 2, 3, 4 (siempre secuencial)
> - `RANK()`: 1, 1, 3 (same rank, next rank salta)
> - `DENSE_RANK()`: 1, 1, 2 (same rank, next rank no salta)

1. **Asignar un número de fila único a cada producto, ordenado por precio (más caro = 1):**

    ```sql
    SELECT 
        product_name, 
        unit_price,
        ROW_NUMBER() OVER (ORDER BY unit_price DESC) AS row_num
    FROM products;
    ```

    -- Explicación: ORDER BY define el orden, cada producto recibe un número secuencial

2. **Clasificar productos por precio dentro de cada categoría (RANK vs DENSE_RANK):**

    ```sql
    SELECT 
        product_name, 
        category_id,
        unit_price,
        RANK() OVER (PARTITION BY category_id ORDER BY unit_price DESC) AS price_rank,
        DENSE_RANK() OVER (PARTITION BY category_id ORDER BY unit_price DESC) AS dense_rank
    FROM products;
    ```

    -- Si hay 2 productos con precioy $15: RANK = 1,1,3 | DENSE_RANK = 1,1,2

### `LEAD` y `LAG`

> **PostgreSQL Tip:** LAG = fila anterior, LEAD = fila siguiente. Útiles para calcular tendencias/cambios entre períodos.
>
> **Parámetros:** LAG(col, offset, default)
>
> - offset: cuántas filas atrás/adelante (default: 1)
> - default: qué mostrar si no hay fila anterior (default: NULL)

1. **Para cada pedido, mostrar la fecha del pedido y la fecha del siguiente pedido del mismo cliente.**

    ```sql
    SELECT 
        customer_id, 
        order_date,
        LEAD(order_date, 1) OVER (
            PARTITION BY customer_id 
            ORDER BY order_date
        ) AS next_order_date
    FROM orders;
    ```

    -- Explicación: Para cada cliente, ordena por fecha y toma la fecha del siguiente pedido

2. **Mostrar el total de una venta y el total de la venta anterior del mismo cliente.**

    ```sql
    WITH customer_sales AS (
        SELECT 
            o.customer_id, 
            o.order_id, 
            o.order_date, 
            SUM(od.quantity * od.unit_price) AS order_total
        FROM orders o 
        JOIN order_details od ON o.order_id = od.order_id
        GROUP BY o.customer_id, o.order_id, o.order_date
    )
    SELECT 
        customer_id, 
        order_id, 
        order_date, 
        order_total,
        LAG(order_total, 1, 0) OVER (
            PARTITION BY customer_id 
            ORDER BY order_date
        ) AS previous_order_total
    FROM customer_sales;
    ```

    -- LAG(order_total, 1, 0): 1 fila atrás, si NULL mostrar 0

### Agregaciones con `OVER`

> **PostgreSQL Tip:** Una función agregada con OVER calcula su resultado manteniendo cada fila. Es como "agregar sin agrupar".
>
> **Ejemplo:** Todos los productos, pero agrego el promedio de su categoría SIN grouping

1. **Mostrar el precio de cada producto junto con el precio promedio de su categoría (sin colapsar):**

    ```sql
    SELECT 
        product_name, 
        unit_price, 
        category_id,
        AVG(unit_price) OVER (PARTITION BY category_id) AS avg_category_price
    FROM products;
    ```

    -- Sin PARTITION BY, usaría todas las filas:
    -- AVG(unit_price) OVER () AS overall_avg

2. **Ejemplo avanzado: Diferencia del precio vs promedio de categoría**

    ```sql
    SELECT 
        product_name, 
        unit_price,
        category_id,
        ROUND(unit_price - AVG(unit_price) OVER (PARTITION BY category_id), 2) AS diff_from_avg
    FROM products
    ORDER BY category_id, unit_price DESC;
    ```

3. **(Extra) Calcular el total de ventas acumulado por mes (PostgreSQL).**

    ```sql
    WITH monthly_sales AS (
        SELECT
            TO_CHAR(order_date, 'YYYY-MM') AS sale_month,
            SUM(od.quantity * od.unit_price) AS monthly_total
        FROM orders o
        JOIN order_details od ON o.order_id = od.order_id
        GROUP BY sale_month
    )
    SELECT
        sale_month,
        monthly_total,
        SUM(monthly_total) OVER (ORDER BY sale_month) AS cumulative_total
        -- ORDER BY sin PARTITION: acumula desde el inicio hasta la fila actual
    FROM monthly_sales;
    ```

58. **(Extra) Encontrar el producto más vendido en cada categoría.**

    ```sql
    WITH product_category_sales AS (
        SELECT
            p.category_id,
            p.product_name,
            SUM(od.quantity) AS total_sold,
            RANK() OVER (PARTITION BY p.category_id ORDER BY SUM(od.quantity) DESC) AS sales_rank
        FROM products p
        JOIN order_details od ON p.product_id = od.product_id
        GROUP BY p.category_id, p.product_name
    )
    SELECT category_id, product_name, total_sold
    FROM product_category_sales
    WHERE sales_rank = 1;
    ```

## 7. `UNION` y `UNION ALL`

**Objetivo:** Combinar el conjunto de resultados de dos o más consultas `SELECT`. `UNION` elimina duplicados, `UNION ALL` no.

1. **Obtener una lista única de todas las ciudades donde viven clientes y proveedores.**

    ```sql
    SELECT city FROM customers
    UNION
    SELECT city FROM suppliers
    ORDER BY city;
    ```

57. **Combinar los nombres de los empleados y los nombres de contacto de los clientes en una sola lista (PostgreSQL).**

    ```sql
    SELECT contact_name FROM customers
    UNION ALL
    SELECT first_name || ' ' || last_name FROM employees;
    ```

## 8. Otros Operadores

### `EXISTS`

**Objetivo:** Comprobar si una subconsulta devuelve alguna fila.

1. **Encontrar todos los clientes que han realizado al menos un pedido.**

    ```sql
    SELECT customer_id, contact_name
    FROM customers c
    WHERE EXISTS (SELECT 1 FROM orders o WHERE o.customer_id = c.customer_id);
    ```

### `ANY` y `ALL`

**Objetivo:** Comparar un valor con un conjunto de valores devueltos por una subconsulta.

1. **Encontrar productos cuyo precio es mayor que CUALQUIER producto de la categoría 1.**

    ```sql
    SELECT product_name, unit_price
    FROM products
    WHERE unit_price > ANY (SELECT unit_price FROM products WHERE category_id = 1);
    ```

## 9. Joins Adicionales

1. **(Extra) Listar las ventas totales por empleado.**

    ```sql
    SELECT e.first_name, e.last_name, SUM(od.quantity * od.unit_price) as total_sales
    FROM employees e
    JOIN orders o ON e.employee_id = o.employee_id
    JOIN order_details od ON o.order_id = od.order_id
    GROUP BY e.employee_id, e.first_name, e.last_name
    ORDER BY total_sales DESC;
    ```

66. **(Extra) Qué clientes han comprado qué categoría de productos.**

    ```sql
    SELECT DISTINCT c.contact_name, cat.category_name
    FROM customers c
    JOIN orders o ON c.customer_id = o.customer_id
    JOIN order_details od ON o.order_id = od.order_id
    JOIN products p ON od.product_id = p.product_id
    JOIN categories cat ON p.category_id = cat.category_id
    ORDER BY c.contact_name, cat.category_name;
    ```
